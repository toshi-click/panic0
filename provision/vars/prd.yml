# 開発用固定の設定をここに書く
# WEBサービス固有の設定値
service:
  # /appの中に置くプロジェクトフォルダ名を定義しておく
  name: "panic0"
  cron_file_path: "/app/panic0/provision/cron/prd/root"
  docker:
    compose_dir: "/app/panic0"

host:
  name: "panic0.toshi.click"
send_slack: "true"

security:
  sudo:
    nopassword: "true"

# sshdの設定
sshd_setting:
  passwordLogin: "no"
  PermitRootLogin: "no"
  # permitUserが空の場合はユーザ制限行いません
  permitUser: ""

# 共通ユーザ
# パスワード生成は「python -c 'import crypt; print crypt.crypt("toshitoshi", "$6$toshitoshi$")'」
manage_user:
  name: toshi
  password: "{{ manage_user_pass }}"
  group: toshi
  gid: 1002
  uid: 1001
  subgroups: sudo

# パスワード生成は「python -c 'import crypt; print crypt.crypt("rootroot", "$6$rootroot$")'」
root_user:
  name: root
  password: "{{ root_user_pass }}"

# hosts.allowに設定される許可アドレス情報
hosts:
  # hosts.allowに設定される許可アドレス情報
  accept_protocol_address:
    # local
    - {protocol: sshd, ip: 127.0.0.1}
    - {protocol: snmpd, ip: 127.0.0.1}
    # CS
    - {protocol: sshd, ip: "122.249.226.152/29"}
    # Home
    - {protocol: sshd, ip: 61.213.68.235}
  # /etc/hostsに設定するホスト情報
  etc_hosts_setting: ""
  # 172.0.0.1を自ホスト名のルールは自動追加しますので不要です

iptables:
  accept_tcp_rules:
    - {port: 80}
    - {port: 443}
    - {port: 22, source: 127.0.0.1}
    # CS
    - {port: 22, source: 122.249.226.152/29}
    # Home
    - {port: 22, source: 61.213.68.235}
    - {port: 25, source: 127.0.0.1}
    - {port: 25, source: 172.16.0.0/12}
    - {port: 25, source: 192.168.0.0/16}
    - {port: 24224, source: 127.0.0.1}
    - {port: 24224, source: 172.16.0.0/12}
    - {port: 24224, source: 192.168.0.0/16}
  accept_udp_rules:
    - {port: 161, source: 127.0.0.1}
  deny_tcp_rules: ""
  deny_udp_rules: ""

app_dir: "/app"
bin_dir: "{{ app_dir }}/bin"
docker_remote_api_enable: "no"
fluentd_user: root
fluentd_group: root
